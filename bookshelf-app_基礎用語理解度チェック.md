# bookshelf-app 基礎用語 理解度チェック

プログラミング学習半年時点での「基本用語・仕組みの理解度」を確認するための問答集です。教科書的な定義だけでなく、実際に自分が書いた bookshelf-app のコードを例に使っているので、「知識として知っている」ではなく「自分のコードで説明できる」を目標にしてください。

使い方：まず問題文だけ読んで自分の言葉で説明してみて、それから解答を確認してください。合っていれば◎、なんとなくでも説明できれば○、詰まったら△をつけておくと、レビュー前にどこを重点的に復習すべきか一目でわかります。

---

## A. オブジェクト指向の基礎

### 1. クラス（class）

**解説1.** 「もの」の設計図です。どんなデータ（プロパティ）を持ち、どんな動作（メソッド）ができるかをまとめて定義したものです。

**Q1.** `app/Models/Book.php` の `class Book extends Model` という1行は、何を宣言していますか？

**A1.** `Book` という名前のクラスを定義し、Laravelの `Model` クラスを継承（`extends`）しています。「書籍」という概念を表す設計図として、`title`（タイトル）や `author`（著者）といったデータを持ち、`genres()` や `reviews()` といった関連データを取得するメソッドを持つ、と定義しています。

### 2. メソッド（method）

**解説2.** クラスの中に定義された「関数」のことです。そのクラス（のインスタンス）に対してできる操作をまとめたものです。

**Q2.** `BookController` の `store()` メソッドは何をするメソッドですか？引数と戻り値の型も含めて説明してください。

**A2.** `public function store(BookRequest $request): RedirectResponse` です。引数として `BookRequest`（バリデーション済みの登録リクエスト）を受け取り、書籍をDBに登録した後、`RedirectResponse`（別ページへのリダイレクト応答）を返すメソッドです。「書籍を1件登録する」という一連の処理をまとめています。

### 3. プロパティ（property）／属性

**解説3.** クラスが持つ「データ」のことです。`Book` クラスで言えば `title` や `author` にあたります。

**Q3.** `Book` モデルの `protected $fillable = ['title', 'author', ...]` は何のためのプロパティですか？

**A3.** マスアサインメント（一括代入）を許可する属性の一覧です。`Book::create($request->validated())` のように配列をまとめて渡してレコードを作成する際、ここに書かれていない属性は代入されません。フォームから送られてきた想定外のデータ（例えば `is_admin` のような属性）が誤って一括代入されてしまう事故（マスアサインメント脆弱性）を防ぐためのホワイトリストです。

### 4. インスタンス（instance）とオブジェクト（object）

**解説4.** クラスという「設計図」から実際に作られた「実体」のことを**インスタンス**と呼びます。同じ `Book` クラスから、書籍Aのインスタンス、書籍Bのインスタンス…と複数作れます。**オブジェクト**は、この「インスタンス」も含めた、もう少し広い言葉です。PHPでは「クラスから作られた実体＝インスタンス＝オブジェクト」とほぼ同じ意味で使われることが多く、「`$book` というオブジェクトは `Book` クラスのインスタンスです」のように、`インスタンス` は「オブジェクト」の中でも特に「どのクラスから作られたか」を強調したいときに使う言葉、というイメージで問題ありません。

**Q4.** `$book = Book::find(3);` を実行したあと、`$book` は「オブジェクト」と「インスタンス」のどちらの言い方もできますが、それぞれの言い方をするとき、何を強調していることになりますか？

**A4.** 「`$book` はオブジェクトだ」と言うときは、単に「これはPHPの値の種類（文字列や数値ではなく）としてオブジェクトである」という点を強調しています。「`$book` は `Book` クラスのインスタンスだ」と言うときは、「数あるクラスの中でも、具体的に `Book` という設計図から作られた実体である」という、**どのクラスに属するか**を強調しています。実務では「`Book` のインスタンス」と言えばより正確に伝わります。

**Q4-2.** `BookController::show(Book $book)` の引数 `$book` は何のインスタンスですか？またそれはどこで作られていますか？

**A4-2.** `Book` クラスのインスタンスで、URLの `{book}` パラメータ（例：`/books/3`）を元にLaravelが自動でDBから該当レコードを検索し、`Book` モデルのインスタンスとして生成してくれています（これを「ルートモデルバインディング」と呼びます。詳しくは6-3で説明します）。自分でコードを書いて検索しなくても、Laravelが自動でやってくれる仕組みです。

### 5. 継承（inheritance / extends）

**解説5.** あるクラスが、別のクラスの機能を引き継ぐことです。`extends` で表現します。

**Q5.** `AuthController extends Controller` や `Book extends Model` のように、bookshelf-appのクラスは軒並み何かを継承しています。継承するメリットは何ですか？

**A5.** Laravelがあらかじめ用意している共通機能（例えば `Model` ならDB操作の仕組み、`Controller` ならリクエスト処理の共通機能）を、自分で1から書かずに使えるようになる点です。`Book` クラスは `Model` を継承しているだけで `Book::create()` や `Book::find()` といったDB操作メソッドが最初から使えます。

---

## B. リクエストが処理される流れ

### 6. ルーティング（routing）

**解説6.** 「どのURL・HTTPメソッドの組み合わせが来たら、どの処理を呼ぶか」を対応付ける仕組みです。`routes/web.php` に定義します。

**Q6.** `Route::get('/books/{book}', [BookController::class, 'show'])->name('books.show');` を日本語で説明してください。

**A6.** 「GETメソッドで `/books/数字` というURLにアクセスがあったら、`BookController` クラスの `show` メソッドを呼び出す」というルールです。`{book}` の部分は可変で、リクエストされたIDに対応する `Book` インスタンスが自動的に `show` メソッドの引数に渡されます。`->name('books.show')` は、このルートに `books.show` という名前をつけていて、Blade側で `route('books.show', $book)` のようにURLを直接書かずに名前で参照できるようにしています。

**Q6-2.**（今回実際にハマったポイント）ルートの定義順序が重要なのはなぜですか？

**A6-2.** Laravelはルートを上から順にマッチングするため、`/genres/{genre}` を `/genres/create` より先に書いてしまうと、`/genres/create` へのアクセスが `{genre}` にマッチしてしまい `"create"` という文字列がIDとして扱われてしまいます。固定パスのルートは、可変パラメータを含むルートより先に定義する必要があります。

### 6-3. ルートモデルバインディング（Route Model Binding）

**解説6-3.** ルートの `{book}` のような可変パラメータを、自動的に対応するEloquentモデルのインスタンスに変換してコントローラに渡してくれるLaravelの機能です。本来なら「URLからIDを取り出す→`Book::find($id)`でDB検索する→見つからなければ404を返す」という定型処理を、自分で毎回書かずに済みます。

**Q6-3.** `Route::get('/books/{book}', [BookController::class, 'show'])` に対して `public function show(Book $book): View` のように引数の型を `Book` と書いているのはなぜ重要ですか？もし存在しないIDでアクセスされたらどうなりますか？

**A6-3.** 引数の型を `Book` にすることで、Laravelに「このパラメータは `Book` モデルとして解決してほしい」と伝えています。これにより `{book}` の値（例：`3`）を主キーとして `books` テーブルを自動検索し、見つかった `Book` インスタンスを渡してくれます。存在しないID（例：`/books/9999`）でアクセスされた場合は、`ModelNotFoundException` が投げられ、Laravelが自動的にそれを404 Not Foundのレスポンスに変換してくれます。自分で `if (!$book) { 404を返す }` のようなコードを書く必要がありません。

### 7. コントローラ（controller）

**解説7.** リクエストを受け取り、必要な処理（DB操作の呼び出しなど）をして、レスポンス（ビューやリダイレクト）を返す「司令塔」の役割を持つクラスです。

**Q7.** コントローラに直接DBの複雑な処理をたくさん書かず、モデルやFormRequest、Policyに処理を分けているのはなぜですか？

**A7.** 責務（役割）を分けるためです。コントローラは「リクエストを受けて、必要な処理を呼び出し、レスポンスを返す」という流れの管理に専念し、バリデーションはFormRequest、DB操作・関連付けの定義はモデル、権限チェックはPolicyに任せることで、それぞれのクラスが「何をしているか」が読みやすくなり、修正やテストもしやすくなります。

### 8. リクエスト（Request）／ FormRequest

**解説8.** ユーザーのブラウザから送られてきたデータ（フォームの入力内容、URLパラメータなど）を表すオブジェクトです。FormRequestはそれに加えて、バリデーションルールを自分で持てるようにしたLaravel独自のクラスです。

**Q8.** コントローラのメソッドで、`Illuminate\Http\Request` ではなく `App\Http\Requests\Book\BookRequest` を型として受け取るとどんなメリットがありますか？

**A8.** メソッドが呼ばれる**前に**、自動的に `BookRequest::rules()` に書かれたバリデーションが実行されます。バリデーションに失敗すればコントローラの処理は実行されず、自動的に前の画面にエラーメッセージ付きで戻ります。コントローラの中にバリデーションのif文を書かなくて済み、`$request->validated()` で「バリデーション済みの安全なデータ」だけを取り出せます。

### 9. レスポンス／ビュー（View）

**解説9.** コントローラの処理結果としてユーザーに返す画面（HTML）やリダイレクト先のことです。BladeテンプレートはLaravelの標準的なビューの書き方です。

**Q9.** `return view('books.index', compact('books'));` は何をしていますか？

**A9.** `resources/views/books/index.blade.php` というBladeテンプレートを描画し、`$books` という変数としてコントローラで取得した書籍一覧データを渡しています。`compact('books')` は `['books' => $books]` という連想配列を作る短縮記法です。

---

## C. Laravel特有の重要概念

### 10. Eloquent（ORM）

**解説10.** SQL文を直接書かなくても、PHPのクラス（モデル）とメソッドの組み合わせでDB操作ができる仕組み（ORM = Object-Relational Mapping）です。

**Q10.** `Book::with('genres')->withAvg('reviews', 'rating')->paginate(9);` を実行すると、裏側でどんなSQLに近いことが行われていますか？

**A10.** 「booksテーブルを取得しつつ、関連するgenresテーブルのデータも一緒に取得し（N+1問題を避けるための事前読み込み）、reviewsテーブルのratingの平均値も一緒に計算し、9件ごとにページ分割する」という処理を、生SQLを書かずにメソッドチェーンで表現しています。

### 11. リレーション（hasMany / belongsTo / belongsToMany）

**解説11.** モデル同士のつながり（DBで言う外部キーの関係）をEloquentのメソッドとして定義したものです。

**Q11.** `Book` モデルの `genres()` メソッドがなぜ `belongsToMany` で、`user()` メソッドがなぜ `belongsTo` なのか説明してください。

**A11.** 1冊の書籍は複数のジャンルに属することができ、1つのジャンルにも複数の書籍が属する（多対多）ため `belongsToMany`（中間テーブル `book_genres` を経由）を使っています。一方、1冊の書籍を登録したユーザーは常に1人（多対1）なので、`user()` は `belongsTo` です。

**Q11-2.** `hasMany` ・ `belongsTo` ・ `belongsToMany` は何が違いますか？bookshelf-appの実際のメソッドを1つずつ例に挙げて、数の関係とDB上の実装（外部キー／中間テーブル）の違いも含めて説明してください。

**A11-2.** 3つとも「モデル同士のつながり」を表す点は同じですが、数の関係とDB実装が異なります。

| 関係 | メソッド | 数の関係 | DB上の実装 | 本プロジェクトの例 |
|---|---|---|---|---|
| 1対多（親側から見た関係） | `hasMany` | 自分1件に対し、相手が複数件ある | 相手のテーブルに外部キーがある（自分のテーブルには何も増えない） | `User::books()`（1人のユーザーが複数の書籍を投稿） |
| 多対1（子側から見た関係） | `belongsTo` | 自分は必ず相手1件に属する | 自分のテーブルに外部キー（`user_id`など）がある | `Book::user()`（1冊の書籍の作成者は1人） |
| 多対多 | `belongsToMany` | 自分・相手どちらも複数件を持ちうる | 双方のテーブルに外部キーは置けないため、中間テーブルが必要（例：`book_genres`） | `Book::genres()` ⇔ `Genre::books()`（1冊が複数ジャンルに属し、1ジャンルに複数冊が属する） |

ポイントは、`hasMany` と `belongsTo` は実は**同じ外部キーを親側・子側それぞれの視点から見ているだけ**という点です。`books` テーブルの `user_id` という1つのカラムに対して、`User` モデル側から見れば「このユーザーが持つ複数の書籍」＝`hasMany`、`Book` モデル側から見れば「この書籍が属する1人のユーザー」＝`belongsTo`、と定義しているにすぎません。一方 `belongsToMany` は、どちらのテーブルにも外部キーを1つだけ置く形では表現できない（1冊が複数ジャンルを持つと、外部キーが複数必要になってしまう）ため、`book_id` と `genre_id` の両方を持つ中間テーブル `book_genres` を別途用意する、という構造上の違いがあります。

### 11-3. N+1問題

**解説11-3.** 一覧表示のように「複数件のデータ」に対して、それぞれの関連データを取得しようとしたとき、**1回の一覧取得クエリ＋件数分の追加クエリ**が発行されてしまい、DBへの問い合わせ回数が無駄に多くなってしまう問題です。例えば書籍が9件あるページで、各書籍のジャンルを表示しようとすると「書籍一覧取得1回＋各書籍ごとにジャンル取得9回＝合計10回」のクエリが発行されてしまう、というイメージです（これが「N+1」＝1回＋N件分、の名前の由来です）。

**Q11-3.** もし `BookController::index()` で以下のように書いていたら、何回クエリが発行される可能性がありますか？（books.blade.php側で `foreach ($books as $book) { $book->genres }` のようにジャンルを表示するとして）

```php
$books = Book::paginate(9); // withなし
```

**A11-3.** 書籍一覧の取得で1回、さらに表示している9件それぞれについて「この本のジャンルは？」というクエリが1回ずつ発行され、合計で最大10回（1 + 9）のクエリが発行されてしまう可能性があります。実際のコードでは `Book::with('genres')->withAvg('reviews', 'rating')->paginate(9)` のように **`with()`（Eager Loading／事前読み込み）** を使っており、「書籍一覧の取得」と「該当する書籍群のジャンルをまとめて取得」の2回程度のクエリで済むようにしています。件数が増えるほど、この差はパフォーマンスに大きく影響します。

### 12. マイグレーション（migration）

**解説12.** DBのテーブル構造（スキーマ）をPHPコードで管理する仕組みです。コードなのでGitで履歴管理でき、チームメンバー間・環境間で同じテーブル構造を再現できます。

**Q12.** `sail artisan test` を実行したときに `RefreshDatabase` トレイトが裏でマイグレーションを使っていますが、これは何をしていますか？

**A12.** テスト実行前に、`database/migrations/` 配下の全マイグレーションファイルを順番に実行して、テスト用DB（SQLiteインメモリ）にゼロから `users`、`books`、`genres` などのテーブルを作成しています。本番用のデータに一切触れずに、毎回まっさらな状態でテストできるようにするためです。

### 13. ミドルウェア（middleware）

**解説13.** リクエストがコントローラに届く**前**（またはレスポンスが返る前）に割り込んで、共通のチェックや処理を行う仕組みです。

**Q13.** `Route::get('/genres/create', ...)->middleware('auth')` の `auth` ミドルウェアは何をしていますか？

**A13.** リクエストがコントローラの `create` メソッドに到達する前に「ログイン済みかどうか」をチェックします。未ログインであれば、コントローラの処理は一切実行されずに、自動的にログイン画面へリダイレクトされます。

### 14. ポリシー（Policy）と「認証」「認可」の違い

**解説14.** 「認証（Authentication）」は**誰であるか**を確認すること（ログインしているか、あなたは誰か）。「認可（Authorization）」は**その人に何をする権限があるか**を確認すること。Policyクラスは認可のルールをまとめたものです。

**Q14.** 認証と認可、それぞれbookshelf-appのどこで行っていますか？

**A14.** 認証はLaravel標準のセッション認証（`Auth::attempt()` など、`AuthController` 内）で行っています。認可は2箇所あり、ページ単位の大まかな制御は `middleware('auth')`（ログインしていること自体のチェック）、個別データに対する詳細な制御（「本人だけが編集できる」）は `BookPolicy::update()` のようなPolicyクラスと、コントローラ内の `$this->authorize()` で行っています。

### 15. バリデーション（validation）

**解説15.** ユーザーが入力したデータが、想定したルール（必須項目・文字数制限・形式など）に沿っているかをチェックすることです。

**Q15.** バリデーションを**フロントエンド（JavaScript）だけ**でやらず、**サーバーサイド（PHP/Laravel）でも**必ず行う必要があるのはなぜですか？

**A15.** フロントエンドのチェックはブラウザの開発者ツールなどで簡単に無効化・回避できてしまうためです。ブラウザを経由しない直接のリクエスト（例えば別のツールから）も送信できてしまうので、サーバー側で必ず検証しないと不正なデータの登録を防げません。フロントのバリデーションはあくまで「ユーザー体験の向上（早めにエラーを教える）」のためのものです。

### 16. ファクトリ（Factory）とシーダー（Seeder）

**解説16.** テストや開発用に、ダミーデータをまとめて生成するための仕組みです。Fakerというライブラリでランダムな値を作れます。

**Q16.** `Book::factory()->for(User::factory())->create(['title' => '低評価の本'])` は何をしていますか？

**A16.** `BookFactory` に定義されたルール（Fakerでダミーの著者名・ISBN・出版日などを自動生成）に従って書籍データを1件作りつつ、`title` だけは明示的に `'低評価の本'` を指定して上書きしています。`for(User::factory())` は、その書籍の作成者となる `User` も同時にダミーで1件作って関連付けています。

### 17. Bladeテンプレート

**解説17.** LaravelのHTMLテンプレートエンジンです。`{{ }}` で変数を表示したり、`@if` `@foreach` `@can` のような制御構文をHTMLの中に書けます。

**Q17.** `books/show.blade.php` の `@can('delete', $review)` は何をしていますか？

**A17.** `ReviewPolicy::delete($user, $review)` を呼び出し、今ログインしているユーザーがそのレビューを削除できるか（本人のレビューかどうか）を判定し、`true` の場合だけ `@can` と `@endcan` の間のHTML（削除ボタン）を出力します。コントローラだけでなく、画面表示の出し分けにも同じPolicyのルールを使い回せます。

### 18. Composer

**解説18.** PHPのパッケージ（ライブラリ）管理ツールです。`composer.json` に必要なライブラリとバージョンを書いておくと、`composer install` で一括インストールしてくれます。npmのPHP版と考えるとイメージしやすいです。

**Q18.** `composer.json` の `"php": "^8.1"` は何を意味していますか？

**A18.** このプロジェクトが動作対象とするPHPのバージョン範囲を示しています。`^8.1` は「8.1以上、9.0未満」を意味し、PHP 8.1〜8.x系であれば動作することを想定しています（実際の開発環境はPHP 8.5です）。

### 19. Sail と artisanコマンド

**解説19.** **artisan**はLaravel本体に付属するコマンドラインツールです。マイグレーション実行、テスト実行、ファイルの雛形生成など、開発作業を自動化してくれます。**Sail**は、そのLaravelアプリを動かすためのPHP・MySQL・その他のミドルウェアを、Docker上に自動で用意してくれるLaravel公式のツールです。自分のMac（ホストOS）に直接PHPやMySQLをインストールしなくても、`docker compose` の複雑な設定を意識せずに開発環境を立ち上げられます。

**Q19.** `./vendor/bin/sail artisan test --stop-on-failure` というコマンドを、1つ1つのパーツに分けて説明してください。

**A19.** `./vendor/bin/sail` がDockerコンテナに対してコマンドを実行するためのLaravel Sailのラッパースクリプトです。`artisan` がLaravel本体のCLIツールを呼び出す部分、`test` がPHPUnit（テスト）を実行するartisanコマンド、`--stop-on-failure` は最初にテストが1件失敗した時点で以降のテストを止めるオプションです。つまり全体で「Dockerコンテナの中に入り、Laravelのartisanコマンドでテストを実行する。ただし1件でも失敗したらそこで止める」という意味になります。Sailを使わずに直接 `php artisan test` と打たないのは、ホストOSにPHPやその実行環境一式が入っていない（またはバージョンが違う）ためです。

### 20. 環境変数（.env）

**解説20.** DBのパスワードやAPIキーなど、環境（本番・開発・テスト）によって変わる設定値を、コードから分離して管理する仕組みです。`.env` ファイルはGit管理対象外にするのが基本です。

**Q20.** テストを実行するとき、`.env` の `DB_CONNECTION=mysql` ではなく `phpunit.xml` で `DB_CONNECTION=sqlite` が使われるのはなぜですか？

**A20.** `phpunit.xml` の `<env>` タグで指定した環境変数は、テスト実行時に `.env` の値より優先して使われます。本番・開発用のMySQLデータベースをテストで汚したり壊したりしないよう、テスト時だけ独立したSQLiteのインメモリDBに切り替えるためにこの仕組みを使っています。

---

## E. 認証まわりの発展的な理解（Fortify・Sanctum）

スクールではFortifyを使った認証を習ったとのことですが、bookshelf-appでは`AuthController`を自作しており、Fortifyは使っていません。また、今後APIとSanctumトークン認証を実装する予定があるとのことなので、「なぜ今回Fortifyを使わなかったか」「Sanctumとは何か・今回の認証と何が違うか」を自分の言葉で説明できるようにしておくと、レビューでの説得力が増します。

### 20-2. Fortify（フォーティファイ）

**解説20-2.** Laravel公式が提供する、認証機能の「ロジック部分だけ」をまとめたパッケージです。ログイン・会員登録・パスワードリセット・メール認証・2段階認証（2FA）といった、Web開発で繰り返し必要になる認証処理のルーティングと処理本体（Actionクラス群）を自動的に用意してくれます。Fortify自体はBladeの画面（見た目）を持たない「ヘッドレス」なパッケージで、Breezeのようなスターターキットや自作のビューと組み合わせて使うのが基本です。

**Q20-2.** FortifyとBreezeは、それぞれ何を提供してくれるパッケージですか？違いを説明してください。

**A20-2.** Breezeは「見た目（Bladeビュー＋Tailwindのレイアウト）」と「認証ロジック」の両方を含む、画面付きのフル装備なスターターキットです。`composer require laravel/breeze` → `php artisan breeze:install` を実行すると、ログイン・会員登録画面一式（コントローラ・ビュー・ルート）がまとめて生成されます。一方Fortifyは、この中の「認証ロジック」の部分だけを切り出した、画面を持たないパッケージです。役割のイメージとしては、Breeze ≒（Fortify相当のロジック）＋（独自のBlade/Tailwindビュー）と捉えると理解しやすいです（実装は別物ですが、住み分けのイメージとして）。より高機能なJetstream（Livewire/Inertia向け）スターターキットも、内部でFortifyを利用しています。

### 20-3. なぜこのプロジェクトではFortifyを使わなかったか

**解説20-3.** 今回実装した認証機能は、会員登録・ログイン・ログアウトのみで、パスワードリセットやメール認証、2段階認証といったFortifyが提供する高度な機能は要件に含まれていませんでした。加えて、認証処理を自分の手で実装することで、Laravelの認証の仕組みそのもの（`Auth::attempt()`が何をしているか、ログイン失敗時にどうやってエラーメッセージをフォームに戻すか、セッション再生成が何のためにあるか等）を理解する、という学習目的も兼ねていました。

**Q20-3.** FortifyやBreezeを使わず`AuthController`・`LoginRequest`・`RegisterRequest`を自作したことで、具体的に何を理解できましたか？また、実務でFortify/Breezeを使うべきなのはどんな場合だと思いますか？

**A20-3.** 自作したことで、「ログイン成功後に`$request->session()->regenerate()`でセッションIDを再発行する理由（セッション固定化攻撃対策）」「認証失敗時に`ValidationException::withMessages()`を投げると、Bladeの`@error`ディレクティブでエラーメッセージが表示される仕組み」「`Auth::attempt()`が内部でパスワードのハッシュ照合をしてくれていること」など、Fortifyが自動でやってくれる処理の中身を実際に手を動かして理解できました。実務では、パスワードリセットやメール認証・2段階認証などが要件に含まれる場合や、開発スピードを優先すべき場合はFortify/Breezeを使う方が合理的だと考えています。今回は「学習目的」と「要件がシンプルな認証で収まっていたこと」から自作を選びました。「車輪の再発明をした」というより「車輪の仕組みを理解するために、要件が小さいうちに一度自分で作ってみた」という位置づけで説明できると、前向きな判断として伝わりやすいです。

### 20-4. Sanctum（サンクタム）と今後のAPI認証計画

**解説20-4.** Laravel Sanctumは、大きく分けて2つの使い方ができる認証パッケージです。①SPA（シングルページアプリ）向けのセッションベース認証、②APIトークン認証（リクエストヘッダーに`Authorization: Bearer <トークン>`を付ける方式）。bookshelf-appは現状、通常のセッション認証（`web`ガード）のみを使ったBladeベースの多ページアプリで、Sanctumはまだ実質的には使っていません。ただし`composer.json`にはLaravelの標準インストール時から`laravel/sanctum`が含まれており、`User`モデルにも`HasApiTokens`トレイトが最初から付いているため、「準備だけはされている」状態です。

**Q20-4.** 今後このアプリにAPIを追加し、SanctumのAPIトークン認証を実装する予定とのことですが、現在のWeb画面のログイン（セッション認証）と、今後実装するAPIトークン認証は、認証の仕組みとして何が違いますか？

**A20-4.** 現在のWeb画面のログインでは、ログイン成功後にサーバーがセッションIDをCookieとしてブラウザに渡し、以降のリクエストではブラウザが自動でそのCookieを送信することで「ログイン状態」を維持しています（`web`ミドルウェアグループ、セッションガード）。一方、今後実装予定のAPIトークン認証では、ログイン時に`$user->createToken('token-name')`のような形でトークン文字列を発行し、APIクライアント（モバイルアプリや外部サービスなど）がリクエストのたびにヘッダーへ`Authorization: Bearer <トークン>`を明示的に付けて送ることで認証します。ブラウザのCookie管理に頼れない・ブラウザを介さないクライアントを想定した認証方式で、`routes/api.php`に`auth:sanctum`ミドルウェアを使ったルートを定義し、Web側とは別の認証の入り口を用意することになります。

**Q20-4b.** なぜWebとAPIで同じ認証の仕組み（セッション）を使い回さず、それぞれ別の方式（セッション vs トークン）にするのですか？

**A20-4b.** Web画面はブラウザがCookieを自動管理してくれる前提で作れますが、外部システムやモバイルアプリなどのAPIクライアントはブラウザを介さないことが多く、Cookieベースの認証やCSRF対策の仕組みと相性がよくありません。APIクライアントにとっては「発行されたトークンを毎回明示的に送る」方式の方が扱いやすいです。Sanctumはこの2方式（セッション／トークン）を1つのパッケージでどちらもサポートしてくれるため、Web用とAPI用で別々の認証パッケージを組み合わせる必要がなく、今回のように「まずWebをセッション認証で作り、あとからAPIをトークン認証で追加する」という拡張の仕方と相性がよい、という理由も説明できるとよいです。

---

## D. 総合問題（全体の流れを説明する）

**Q21.** ログイン済みユーザーが書籍詳細ページ（`/books/3`）でレビューを削除するボタンを押してから、画面が更新されるまでの流れを、ここまでの用語（ルーティング／ミドルウェア／コントローラ／ポリシー／Eloquent／ビュー）を使って説明してください。

**A21.** の組み立て方（模範解答の骨子）：

1. ブラウザから `DELETE /reviews/{review}` にリクエストが送られる（Blade側でフォームに `@method('DELETE')` を仕込んでいる）。
2. **ルーティング**：`routes/web.php` の `Route::middleware('auth')->group(...)` 内で定義された `reviews.destroy` にマッチする。
3. **ミドルウェア**：`auth` ミドルウェアが「ログイン済みかどうか」をチェックする（このルートはグループ全体が `auth` 配下）。
4. **コントローラ**：`ReviewController::destroy(Review $review)` が呼ばれる。Laravelがルートモデルバインディングで `$review` にDBから取得した `Review` インスタンスを渡す。
5. **ポリシー**：コントローラ内の `$this->authorize('delete', $review)` が `ReviewPolicy::delete()` を呼び、本人のレビューかどうかを確認する。権限がなければここで403エラーになる。
6. **Eloquent**：`$review->delete()` でDBからレコードが削除される。
7. **レスポンス**：`back()->with('success', ...)` で元のページにリダイレクトされ、セッションに成功メッセージが積まれる。
8. **ビュー**：リダイレクト先の `books/show.blade.php` が再度描画され、削除後のレビュー一覧（と成功メッセージ）が表示される。

---

## 使い方の補足

一度に全部を完璧にしようとせず、△をつけた項目だけを毎日数個ずつ、実際の該当コード（ファイル名・行）を開きながら音読するのが定着しやすいです。レビュー当日に緊張して忘れても、「自分のコードのどのファイルに書いてあるか」さえ思い出せれば、その場でコードを見ながら説明できます。
