# bookshelf-app レビュー想定問答集

このドキュメントは、コーチテック課題「bookshelf-app」のレビューに向けて、自分の実装内容を自分の言葉で説明できるようにするための想定問答集です。AIと一緒に開発を進めた部分も含め、「なぜそう作ったか」を自分の理解として説明できることを目標にしています。

丸暗記するのではなく、まず一度通しで読んで、答えられなさそうなところに印をつけて、そこだけ何度か声に出して説明する練習をするのがおすすめです。

---

## 1. プロジェクト全体像

**Q. このアプリは何ができるアプリですか？**

A. 書籍のレビュー・本棚管理アプリです。ユーザーは会員登録・ログインした上で、書籍の登録・編集・削除、ジャンルの管理、書籍へのレビュー投稿（5段階評価＋コメント）、レビューへの「いいね」、書籍のお気に入り登録、平均評価によるランキング表示ができます。書籍一覧・詳細・ジャンル一覧・ランキングは未ログインでも閲覧できますが、登録・編集・削除系の操作にはログインが必要です。

**Q. 技術構成を教えてください。**

A. Laravel 10（PHP 8.5）、MySQL 8.4、Docker（Laravel Sail）で構築しています。フロントは素のBlade＋Tailwind CSS、Alpine.js（Breezeスタックのレイアウトをベースにしています）で、SPA的なJSフレームワークは使っていません。認証はLaravel標準のセッション認証です。

**Q. なぜBladeで作ったのですか？SPA（Vue/React）にしなかった理由は？**

A. 課題要件がサーバーサイドレンダリングのアプリケーションだったこと、また認可・バリデーション・DB設計といったバックエンドの基礎を重点的に学ぶ課題だったため、フロントの複雑さを持ち込まずBladeで完結させました。

---

## 2. データベース設計

**Q. テーブル構成を説明してください。**

A. 主に7テーブルです。
- `users`（ユーザー）
- `books`（書籍：title, author, isbn, published_date, description, image_url, user_id）
- `genres`（ジャンル：name）
- `book_genres`（books と genres の中間テーブル、多対多）
- `reviews`（書籍へのレビュー：user_id, book_id, rating, comment）
- `favorites`（お気に入り、users と books の中間テーブル）
- `review_likes`（レビューへのいいね、users と reviews の中間テーブル）

**Q. 書籍とジャンルの関係はなぜ多対多にしたのですか？**

A. 1冊の本が複数ジャンルに属しうる（例：ミステリーかつサスペンス）ことと、1つのジャンルに複数の本が属することを想定したためです。中間テーブル `book_genres` で `book_id` と `genre_id` の複合主キーを持たせ、同じ組み合わせが重複登録されないようにしています。

**Q. `favorites` や `review_likes` にはなぜ `id` カラムがなく複合主キーなのですか？**

A. これらは「あるユーザーがある書籍/レビューに対して1回だけ」成立する関係で、複合主キー自体が一意制約の役割を兼ねるため、サロゲートキー（連番のid）を持たせる必要がないと判断しました。同じユーザーが同じ書籍を二重にお気に入り登録できないことをDBレベルでも保証できます。

**Q. `reviews` テーブルの `unique(['user_id', 'book_id'])` は何のためですか？**

A. 「1人のユーザーは1冊の書籍に対して1件しかレビューを投稿できない」という業務ルールをDBレベルで保証するためです。アプリ側（`ReviewPolicy::create`）でも同じ制約をチェックしていますが、二重投稿を確実に防ぐためにDB側にも制約を持たせる、という二重の防御にしています。

> 実は開発中、テストコードでレビューを2件作ろうとした際にこのユニーク制約に引っかかったことがありました（Factoryで同じユーザーが使い回されてしまっていたのが原因）。制約が正しく効いている証拠でもあります。

**Q. 外部キーの `cascadeOnDelete` と `restrictOnDelete` を使い分けている理由は？**

A. 書籍を削除したときは、その書籍に紐づくレビュー・お気に入り・ジャンルの紐付け（book_genres）も一緒に消えてほしいので `cascadeOnDelete` にしています。一方、ジャンル側（`book_genres.genre_id`）は `restrictOnDelete` にしていて、書籍に紐づいているジャンルは削除できないようにしています。ジャンルはマスタ的なデータなので、うっかり削除して既存書籍のジャンル情報が消えてしまう事故を防ぐ意図です。

**Q. ジャンル削除時、事前に紐づく書籍数をチェックせず、例外（QueryException）を catch する実装になっていますが、なぜですか？**

A. `GenreController::destroy()` では `restrictOnDelete` 制約に違反した際にDBが返す `SQLSTATE 23000` を catch して、ユーザー向けのエラーメッセージに変換しています。事前に `$genre->books()->count()` でチェックする方法も可能ですが、チェックと削除の間に別のリクエストで書籍が紐づけられる可能性（競合状態）を考えると、DBの制約に最終判断を委ねて例外を捕捉する方が確実だと考えました。

---

## 3. 認証・認可

**Q. 認証はどう実装していますか？**

A. Laravel標準のセッションベース認証（`Auth::attempt` / `Auth::login` / `Auth::logout`）を使っています。パスワードは `User` モデルの `$casts` に `'password' => 'hashed'` を指定しており、代入時に自動でハッシュ化されます（Laravel 10から使える機能です）。

**Q. 認可（誰が何をしてよいか）はどう管理していますか？**

A. 2つのレイヤーで管理しています。
1. **ルートミドルウェア**：ログインしていないと到達できないページ（書籍登録、ジャンル登録・編集・削除など）には `middleware('auth')` を付けています。
2. **Policy（`BookPolicy` / `ReviewPolicy`）**：「本人だけが編集・削除できる」といった、対象データに紐づく認可はPolicyクラスで判定し、コントローラで `$this->authorize()` を呼んで検証しています。例えば書籍の更新は `BookPolicy::update()` で `$user->id === $book->user_id` を見ています。

**Q. FormRequestの `authorize()` メソッドが軒並み `true` を返していますが、認可をやっていないのですか？**

A. やっています。FormRequestの`authorize()`は「このリクエスト自体を送ってよいか」という入り口のチェックで、今回はバリデーション用途と役割を分け、実際の権限チェック（本人確認など）はコントローラ側で明示的に `$this->authorize()`（Policy）を呼ぶ形に統一しました。バリデーションと認可の責務を分離した設計です。

**Q. `ReviewPolicy::create` の実装を説明してください。**

A. `! $book->reviews()->where('user_id', $user->id)->exists()` としていて、「そのユーザーがその書籍にまだレビューしていなければtrue（投稿可）」を返します。これは前述の `reviews` テーブルのユニーク制約と同じルールをアプリ層でも表現したもので、ユーザーには「未達成のバリデーションエラー」ではなく「この本にはすでにレビュー済みです」に近いUXを提供する意図もあります（実際の画面制御は show.blade.php 側の `$hasReviewed` で行っています）。

**Q. `BookPolicy` / `ReviewPolicy` に `viewAny` `view` `restore` `forceDelete` という中身が空のメソッドがありますが、これは何ですか？**

A. `php artisan make:policy --model` で生成した際の雛形（Laravelの標準スタブ）がそのまま残っているものです。書籍一覧・詳細は認可なしで誰でも見られる仕様なので `viewAny` `view` は使っておらず、論理削除（ソフトデリート）も実装していないため `restore` `forceDelete` も未使用です。今回は動作に影響しないため残していますが、レビューで指摘されたら「未使用コードなので削除する」と即答できるようにしておきたいポイントです（実際、コードレビューの過程でも指摘を受け、削除するかどうか検討していた箇所です）。

---

## 4. バリデーション設計

**Q. バリデーションをFormRequestクラスに分離した理由は？**

A. コントローラのメソッドをリクエストの受け取り・処理に専念させ、入力チェックのルールとエラーメッセージをFormRequestに集約するためです。可読性と再利用性（登録・更新で同じルールを使い回せる）が上がります。

**Q. 書籍やジャンルの更新時、`Rule::unique(...)->ignore($book)` としているのはなぜですか？**

A. 例えば書籍タイトルの一意性チェックで、何も指定しないと「自分自身の現在の値」でも重複エラーになってしまいます。更新対象自身のレコードをチェック対象から除外するために `ignore()` を使っています。`$book = $this->route('book')` でルートパラメータから対象モデルを取得し、新規登録時（`$book` が null）はそのまま全件対象に、更新時は自分自身を除外、という1つのルール定義で両方に対応させています。

**Q. `image_url` や `description` を `Book::create()` / `update()` で `?? null` としている箇所がありますが、なぜですか？**

A. これらは任意項目（`nullable`）のため、フォームが未入力の場合バリデーション後の `$validated` 配列にそのキー自体が存在しないことがあります。存在しないキーに直接アクセスすると `Undefined array key` エラーになるため、`?? null` で「キーがなければnullを使う」形にして、未入力でも安全に登録・更新できるようにしています。

> これは実際に開発中デグレとして踏んだバグでした。`ReviewController` の方は最初から同じパターンで正しく書けていたのに、`BookController` だけ抜けていて気づいた、という経緯があります。「nullable項目をcreate/updateに渡すときは `??` を忘れない」という学びとして説明できるとよいです。

---

## 5. Eloquentリレーション・パフォーマンス

**Q. N+1問題への対策はしていますか？**

A. 一覧・詳細表示で `with()` / `withAvg()` / `withCount()` を使い、Eager Loadingしています。例えば書籍一覧（`BookController::index`）では `Book::with('genres')->withAvg('reviews', 'rating')->paginate(9)` としており、書籍ごとにジャンルや平均評価を都度クエリせずに済むようにしています。ランキングも同様に `withAvg` / `withCount` を使っています。

**Q. `withAvg('reviews', 'rating')` は何をしていますか？**

A. 各書籍について `reviews.rating` の平均値を1回のクエリ（サブクエリ的なJOIN）でまとめて取得し、`reviews_avg_rating` という属性としてモデルに付与します。N+1を避けつつ平均評価をソートに使えるようにしています（ランキングでは `orderByDesc('reviews_avg_rating')` で使用）。

**Q. ランキングの並び順ロジックを説明してください。**

A. `withAvg`で平均評価、`withCount`でレビュー件数を取得した上で、`whereHas('reviews')`でレビューが1件もない書籍を除外し、平均評価の降順→同点ならレビュー件数の降順→それも同点なら出版日の新しい順、で上位10件を表示しています。同着の扱いをどうするかは要件に明記がなかったため、自分で「レビュー数が多い方を信頼度が高いとみなす」という基準を決めて実装しました。

**Q. お気に入り／いいねの「トグル」処理はどう実装していますか？**

A. `$user->favoriteBooks->contains($book->id)` で既に登録済みかを判定し、登録済みなら `detach()`、未登録なら `attach()` を呼ぶ形で、1つのエンドポイント（POST）で登録・解除の両方を切り替えています。中間テーブルへの単純なattach/detachなので、DELETEメソッドを分けずにシンプルにしました。

---

## 6. ルーティング設計

**Q. ルート定義で気をつけたことはありますか？**

A. Laravelはルートを**上から順に**マッチングするため、`/genres/{genre}` のような可変パラメータを持つルートを、`/genres/create` のような固定パスのルートより先に書いてしまうと、`create` という文字列が `{genre}` パラメータとして解釈されてしまいます。

実際に開発中、ジャンル側でこの順序を誤ってしまい、未ログインユーザーが `/genres/create` にアクセスした際、本来は「authミドルウェアでログイン画面にリダイレクト」されるべきところ、先に `/genres/{genre}` にマッチして `genre_id = "create"` のジャンルを探しにいき、見つからず404になる、というバグを起こしました。書籍側（`/books/create` を `/books/{book}` より先に定義）は最初から正しい順序だったため問題が出ませんでした。この経験から「固定パスは可変パラメータより先に定義する」というルールを意識するようになりました。

---

## 7. テスト設計

**Q. テストはどういう方針で作りましたか？**

A. PHPUnitのFeatureテストを、機能の大項目（会員登録・ログイン・ログアウト・書籍・ジャンル・レビュー・お気に入り・ランキング）ごとにディレクトリを分け、さらに操作（一覧・登録・更新・削除など）単位でファイルを分割しています（`tests/Feature/{Auth,Book,Genre,Review,Favorite,Ranking}/`）。大きな1ファイルにまとめるより、機能単位で見通しよく管理・実行できることを優先しました。

**Q. `RefreshDatabase` トレイトは何のために使っていますか？**

A. 各テストの実行前にマイグレーションを流してテスト用DBをまっさらな状態にし、各テストメソッドの終わりにトランザクションをロールバックすることで、テスト間でデータが混ざらないようにするためです。テスト用DBには `phpunit.xml` でSQLiteのインメモリDB（`:memory:`）を指定していて、本番・開発用のMySQLとは完全に切り離しています。

**Q. なぜSQLiteのインメモリDBをテスト用に使ったのですか？MySQLでテストしないのですか？**

A. テスト実行の速さと、テスト用DBを毎回作り直すコストの低さが理由です。本番はMySQLですが、今回のテストで使っている機能（基本的なCRUD、外部キー制約、ユニーク制約）はSQLiteでも同じように再現できるため、テストの独立性・速度を優先してインメモリDBを選びました（MySQL固有の機能に依存する処理があれば別途MySQLでのテストが必要になりますが、本アプリでは該当しません）。

**Q. Factory（`BookFactory` など）はどう使っていますか？**

A. テスト用のダミーデータを生成するために使っています。当初は空のスタブだったため、Fakerを使って実データ（タイトル・著者・ISBN・出版日など）を生成するよう実装しました。テストでは `Book::factory()->for(User::factory())->create([...])` のように、関連モデルごと必要な分だけ生成しています。

**Q.（応用）`Review::factory()->count(2)` と `for(User::factory())` を組み合わせたときに何が起きましたか？**

A. `count()` と `for(関連ファクトリ)` を併用すると、関連先（この場合はUser）が1回しか生成されず、複数件の作成すべてで同じユーザーが使い回されてしまいます。ランキングのテストで「同じ書籍に2人が別々にレビューする」状況を作ろうとして `count(2)` を使ったところ、実際には同一ユーザーが同じ本に2回レビューしようとする形になり、`reviews` テーブルのユニーク制約違反でテストが失敗しました。修正として、`create()` を2回に分けてそれぞれ別に `User::factory()` を評価させる形にしました。

**Q. アサーション（`assertSee` / `assertDontSee`）でハマったことはありますか？**

A. `books.show` のビューで、レビューの編集・削除ボタン部分に `<!-- 編集・削除ボタン -->` という通常のHTMLコメントを書いていました。実際のボタン自体は `@can('delete', $review)` で権限制御されていたのですが、このコメントは権限に関係なく常にレスポンスのHTMLソースに出力されます。`assertDontSee('削除')` は画面の見た目ではなくレスポンス本文に文字列が含まれるかどうかを見るため、このコメントの文字だけで「他人はボタンが見えないはず」のテストが誤って失敗しました。HTMLコメント（`<!-- -->`）をBladeコメント（`{{-- --}}`）に変えることで解決しました。Bladeコメントはコンパイル時に完全に除去され、レスポンスに一切出力されないためです。この経験から、`assertSee`系のテストがある画面では、表示制御していない文言（コメント含む）にも同じ単語を不用意に含めないよう気をつける必要がある、と学びました。

**Q. テストカバレッジはどのくらいですか？**

A. 実測はまだですが（pcovを使って `sail artisan test --coverage` で計測できる状態にはしています）、Feature/Unitあわせて約100メソッドのテストを、要件のテストケース一覧（Excel）と突き合わせながら作成したので、主要な正常系・異常系・権限まわりのシナリオは大きな抜け漏れなくカバーできていると考えています。

---

## 8. 実装上の割り切り・今後の改善点として説明できるとよい点

レビューで「ここはどうなっていますか」と聞かれたときに、正直に「現状こうで、改善するなら〜」と言えるようにしておくと印象がよいです。

- **`BookPolicy` / `ReviewPolicy` の未使用スタブメソッド**（`viewAny` / `view` / `restore` / `forceDelete`）：使っていないので削除してよい。ソフトデリートを導入する場合は `restore` / `forceDelete` が必要になる。
- **ランキングの上限が10件固定**：`take(10)` とハードコードしている。件数を可変にするならクエリパラメータ化するのが自然な拡張。
- **お気に入り・いいねのUIフィードバック**：現状はページ全体をリロード（`back()`）する作りで、Ajax化すればUXは向上する。
- **画像アップロード非対応**：`image_url` はURL文字列のみで、ファイルアップロード機能は未実装。
- **テストカバレッジの実測が未実施**：見込みでは大きな漏れはなさそうだが、数値としては未計測。

---

## 9. 「AIを使って開発したこと」について聞かれたら

正直に答えることが一番大事です。隠したり、逆に卑下しすぎたりせず、以下のような軸で説明すると誠実に伝わります。

**Q. AIをどのように使いましたか？**

A. 実装のたたき台作成やデバッグの相談相手として使いました。特にテストコードの一括作成、テスト実行時に出たエラーの原因調査（ルーティングの順序ミス、Factoryの仕様、Bladeコメントの扱いなど）で活用しています。ただし、DB設計（テーブル構成・制約の意図）や、どこにPolicy/ミドルウェアで認可を置くか、バリデーション仕様といった**設計判断は自分で決めて指示した**上で、AIには実装・デバッグを手伝ってもらう、という使い方をしました。

**Q. AIが直したバグについて、自分で説明できますか？**

A. （このドキュメントの各セクションで扱った内容がまさにこれです）はい。単に直してもらって終わりにするのではなく、「なぜそのエラーが起きたのか」を毎回確認しながら進めたので、原因と修正内容は自分の言葉で説明できます。

**Q. 今後AIなしで同じレベルのものを書けますか？**

A. 設計判断（テーブル構成、認可の置き場所、バリデーション設計など）は自分で行えます。Laravelの細かい仕様（ルーティングのマッチング順序、Factoryの`count()`と`for()`の組み合わせの挙動、Bladeコメントの挙動など）は今回の経験で実際につまずいて理解できたので、次に同じ問題が起きたときは自力で気づけると思います。

---

## 使い方の補足

このファイルは印刷して手元に置いたり、声に出して1問1答を練習したりする用途を想定しています。特に「8. 実装上の割り切り」と「9. AIを使ったことについて」は突っ込まれやすいポイントなので重点的に確認しておくとよいです。
